# Product Spec — ASIN 智能分类系统

## 产品概述

这是一个面向亚马逊运营团队的 ASIN 智能分类工具。系统接收上万级商品数据（标题、属性、主图），通过「小样本标注 + 多模态 AI 分类」自动将商品精准划分到特定细分品类或风格中。

**目标用户**：2-5 人的亚马逊运营/市场分析团队。他们每天需要处理大量 ASIN 的品类归类工作——区分"农舍风转盘"和"波西米亚风转盘"、排除"根本不是转盘"的干扰项。目前完全靠人工逐个查看，上万条数据要盯好几天，效率极低且标准不统一。

**核心价值**：运营人员只需标注 3-5 个样本，系统自动处理剩余上万条 ASIN。通过"AI 初筛 → 大模型精判 → 人工兜底"三层漏斗，将分类准确率做到 90%+，人工时间从几天缩短到 1-2 小时。而且越用越准——每次复核纠正都在帮系统学习。

## 应用场景

- **新品类调研**：运营小李需要调研"转盘"(Lazy Susan) 这个品类的市场格局。他导出 12,000 个相关 ASIN，标注 15 个样本（农舍风、波西米亚风、现代风、非转盘各几个），系统 30 分钟跑完分类。他花 1 小时复核少量不确定项，拿到完整的品类分布报告。

- **竞品风格分析**：团队想了解某品类下的风格分布占比。导入数据后，系统按风格分类，导出的 Excel 直接包含每个 ASIN 的风格标签和置信度。运营用这个数据做竞品分析报告。

- **批量排除干扰项**：筛选"花瓶"品类时，搜索结果里混了大量"假花"、"花盆"、"装饰品"。标注几个反面案例后，系统自动过滤掉非花瓶商品，准确率远高于关键词匹配。

- **日常补充分类**：上周跑过一轮的"窗帘"项目，本周新增了 800 个 ASIN。直接在已有项目里追加数据，系统用积累的 50+ 样本直接分类，准确率 95%+，几乎不需要人工介入。

- **团队分工协作**：小张负责"厨房用品"品类，小王负责"家居装饰"品类。各自创建独立项目，互不干扰，每个项目积累自己的样本和经验。

## 功能需求

### 核心功能

- **项目管理**：用户创建/查看/删除分类项目 → 每个项目对应一个品类分析任务，独立维护样本库和分类结果 → 项目列表展示名称、ASIN 数量、最近运行时间、样本充分度指标

- **数据导入**：用户上传 Excel/CSV 文件或粘贴 ASIN 列表 → 系统解析 SKU、标题、属性、图片链接等字段 → 展示数据预览，确认后入库

- **类别定义与样本标注**：用户为项目定义分类类别（如"农舍风""波西米亚风""非转盘"）→ 在商品卡片上标注每个类别的正例和反例 → 系统展示商品图片+标题+属性供标注参考 → 每个类别建议至少标注 3 个正例

- **智能分类**：用户点击"开始分类"→ 系统对未标注的 ASIN 执行三层分类（Embedding 相似度 → 大模型精判 → 标记待审核）→ 实时展示处理进度和已分类数量 → 支持分批处理（先跑 500 条，复核优化后再跑全量）

- **分类结果查看与复核**：用户按类别/置信度/来源筛选结果 → 查看每个 ASIN 的分类标签、置信度分数、判定来源 → 一键改判（纠正的分类自动成为新样本）→ 标记"待人工审核"的商品单独筛选出来处理

- **结果导出**：用户点击"导出"→ 系统生成 Excel 文件，包含 ASIN、SKU、分类结果、置信度、判定来源（向量/AI/人工）、原始标题等字段

### 辅助功能

- **样本充分度提示**：系统自动评估每个类别的样本是否足够代表性 → 不够时提示"建议再标注 X 个样本" → 评估基于样本数量、特征多样性、类别间区分度

- **降噪词库管理**：系统从数据中自动统计高频噪声词 → 用户可手动添加/删除降噪词 → 每个项目维护独立的降噪词库

- **置信度阈值调整**：用户可调整三层漏斗的阈值（高/中/低置信度分界线）→ 调高阈值则更多商品走免费向量分类（省钱但准确率略降），调低则更多走大模型精判（花钱但更准）

- **项目经验复用**：同一项目新增 ASIN 时，直接使用已有样本库和原型向量 → 不需要重新标注 → 分类从一开始就高准确率

## UI 布局

### 整体布局

单栏全宽布局，顶部固定导航栏，下方为各页面内容区。导航栏包含项目切换和全局操作。

### 导航栏

- 左侧：系统名称"ASIN 智能分类"
- 中间：当前项目名称（可切换）
- 右侧：全局设置按钮

### 页面 1：项目管理（首页）

- 页面标题"我的项目"
- 右上角"创建新项目"按钮（主色调，醒目）
- 项目卡片网格（2-3 列），每张卡片包含：
  - 项目名称
  - ASIN 总数 / 已分类数 / 待审核数
  - 样本充分度进度条
  - 最近运行时间
  - 快捷操作：进入项目 / 导出结果 / 删除

### 页面 2：数据导入

- 顶部面包屑导航：项目名 > 导入数据
- 文件上传区域：拖拽上传 Excel/CSV，或点击选择文件
- 粘贴输入区域：文本框，支持粘贴 ASIN 列表（每行一个或逗号分隔）
- 数据预览表格：显示前 20 行数据，列包括 ASIN、SKU、标题、属性、图片链接
- 底部：已解析数量统计 + "确认导入"按钮

### 页面 3：样本标注

- 顶部面包屑导航：项目名 > 标注样本
- 左侧（40%宽）：类别管理面板
  - 类别列表，每个类别显示名称和已标注样本数
  - "添加类别"按钮
  - 点击类别可展开查看/删除已标注样本
- 右侧（60%宽）：商品标注区
  - 商品卡片：大图预览 + 标题 + 属性
  - 类别选择：按钮组，每个类别一个按钮
  - "标为反例"复选框
  - 左右翻页浏览未标注商品
  - 快捷键支持：1-9 对应类别，0 标为反例
- 底部：样本统计（每类几个正例/反例）+ "样本充足，开始分类"按钮

### 页面 4：分类控制台

- 顶部面包屑导航：项目名 > 分类控制台
- 统计卡片行：ASIN 总数 / 高置信数 / AI精判数 / 待审核数
- 分批控制面板：
  - 输入框：本次处理数量（默认 1000）
  - 滑块：置信度阈值（高/中/低分界线）
  - "开始分类"按钮
- 处理进度条 + 实时日志（最近 10 条处理记录）
- 预估成本显示：基于当前批量和阈值，预估本次花费

### 页面 5：分类结果与复核

- 顶部面包屑导航：项目名 > 分类结果
- 统计面板：
  - 各类别数量柱状图
  - 置信度分布饼图
  - 来源分布（向量/AI/人工占比）
- 筛选栏：按类别 / 置信度（高/中/低）/ 来源（向量/AI/待审核）筛选
- 结果表格：
  - 列：缩略图 | ASIN | 标题(截断) | 分类结果 | 置信度 | 来源 | 操作
  - 操作列：[改判] 按钮（点击弹出类别选择下拉框）/ [确认] 按钮
- 分页：每页 50 条
- 底部固定栏："导出 Excel"按钮 + 已选中数量 + 批量操作（批量确认/批量改判）

### 页面 6：设置页

- 置信度阈值调整（三个滑块：高置信下限、中置信下限）
- 降噪词库管理（添加/删除/查看统计）
- AI 模型选择（文本 Embedding / 图片模型 / 精判模型）
- 特征融合权重（文本/图片比例滑块）

## 用户使用流程

### 快速上手（首次使用）

1. 创建新项目，输入项目名称（如"转盘分类"）
2. 上传 Excel 文件导入 ASIN 数据，确认预览无误
3. 定义分类类别（农舍风、波西米亚风、现代风、非转盘）
4. 逐个浏览商品，为每个类别标注 3-5 个正例和 2-3 个反例
5. 点击"开始分类"，首次建议跑 500-1000 条
6. 等待处理完成（约 5-10 分钟）
7. 查看结果，重点复核"待审核"和"AI精判"的商品
8. 纠正错误分类（一键改判，系统自动学习）
9. 再次点击"开始分类"跑更多数据，准确率已提升
10. 全部完成后导出 Excel

### 日常补充（已有项目追加数据）

1. 进入已有项目
2. 追加导入新的 ASIN 数据
3. 直接点击"开始分类"（系统使用积累的样本，无需重新标注）
4. 复核少量不确定项
5. 导出新增部分的分类结果

### 调优流程

1. 查看分类结果中的"样本充分度"提示
2. 如果某类别准确率低，进入标注页面补充更多样本
3. 调整置信度阈值（想要更省钱就调高阈值，想要更准就调低）
4. 调整特征融合权重（如果发现图片比文本更能区分，提高图片权重）
5. 重新跑分类，对比前后准确率变化

## AI 能力需求

| 能力类型 | 用途说明 | 应用位置 |
|---------|---------|---------|
| 文本 Embedding | 将商品标题+属性转为高维向量，捕捉语义特征 | 数据导入后自动提取 / 每次分类前 |
| 图片 Embedding (CLIP) | 将商品主图转为向量，捕捉视觉特征 | 数据导入后自动提取（云端 GPU） |
| 向量相似度计算 | 计算商品向量与类别原型向量的余弦相似度 | 分类流程第一层 |
| 大模型文本分类 | 对边界案例进行精确分类判断 | 分类流程第二层（中置信度商品） |
| 降噪与特征提取 | 清洗标题中的堆砌词，提取有效特征 | 数据导入时自动处理 |

## 技术方向

| 维度 | 选择 | 理由 |
|------|------|------|
| 产品类型 | Web 应用 | 浏览器操作，团队共享，无需安装 |
| 前端技术栈 | Next.js + React + TailwindCSS | 全栈框架，生态成熟，开发效率高 |
| 后端技术栈 | Python + FastAPI | AI/ML 生态最好，所有工具库原生支持 |
| 数据存储 | 本地 SQLite + ChromaDB | 轻量零配置，小团队单机够用 |
| 文本 Embedding | OpenAI text-embedding-3-small | 成本极低（$0.02/百万 token） |
| 图片 Embedding | CLIP ViT-B/32（云端 GPU） | 免费模型，按需付费，不占本地资源 |
| 大模型精判 | GPT-4o-mini / Claude Haiku | 最便宜的可用模型，边界案例够用 |
| 云端 GPU | Replicate / Modal | 按秒计费，跑完即释放 |
| 部署方式 | 本地 Docker | 小团队内部使用，无需公网部署 |

## 技术说明

- **外部依赖**：
  - OpenAI Embedding API（文本向量化）
  - Replicate / Modal（云端 GPU 跑 CLIP）
  - OpenAI / Anthropic API（大模型精判）
  - 三个 API 均需要对应的 API Key

- **数据隐私**：商品数据仅用于本地分类，不会泄露给第三方。API 调用仅发送必要的文本和图片 URL。

- **性能要求**：1 万 ASIN 的分类任务应在 30 分钟内完成（含 Embedding 提取、分类计算、大模型精判）。

- **成本控制**：默认配置下，1 万 ASIN 约 $3-7。用户可通过调整置信度阈值控制成本。

## 补充说明

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 置信度-高置信下限 | 0.85 | 高于此值直接采用向量分类结果 |
| 置信度-中置信下限 | 0.65 | 低于高置信但高于此值走大模型精判 |
| 特征融合权重（文本） | 60% | 文本向量在融合特征中的权重 |
| 特征融合权重（图片） | 40% | 图片向量在融合特征中的权重 |
| 每批默认处理量 | 1000 | 分批处理时每批的 ASIN 数量 |
| 每页显示数量 | 50 | 结果列表每页展示的条数 |
| 建议最少样本数 | 3 | 每个类别建议标注的最少正例数 |

| 分类来源 | 含义 | 标签颜色 |
|---------|------|---------|
| embedding | 向量相似度直接判定 | 绿色 |
| llm | 大模型精判判定 | 蓝色 |
| human | 人工手动判定 | 橙色 |
| pending | 待人工审核 | 红色 |
