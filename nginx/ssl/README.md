Place production TLS files here before starting the `edge` profile:

- `tls.crt`: full certificate chain for the public domain
- `tls.key`: private key for the certificate

Do not commit real certificate private keys.
