{
  "name": "sparkloom-agent",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Local Sparkloom Agent for WebSocket + Claude Agent SDK execution",
  "scripts": {
    "start": "node src/index.mjs",
    "check": "node --check src/index.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.193"
  }
}
